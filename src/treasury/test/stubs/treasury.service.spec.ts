describe('Testing the treasury service', () => {

    it('Creates a new record with the required fields', async () => {

    })

    it('Executes on a cron timer passed in as an argument', async () => {

    })

    it('Makes an outbound request to the Zapper API', async () => {

    })

    it('Fetches our data when requested', async () => {

    })
})